# Generalized composition plan

1. Extend optional source hints with coarse zones, grouping, ordering, columns,
   ratios, flow direction, and importance. Validate references and teach image
   extraction to preserve structure without guessing uncertain coordinates.
2. Analyze canonical semantics plus hints into a deterministic, flex-ready
   blueprint. Explicit geometry/grouping wins; missing hints preserve source order
   and section grammar in a conservative full-width stack.
3. Render unknown inputs through nested Flexbox regions while reusing existing
   table, process, comparison, timeline, checklist, metric, cycle, callout, and list
   primitives. Keep five portfolio templates as higher-confidence specializations.
4. Measure composition fidelity and actual Satori geometry. Auto-grow height or
   reject impossible fixed canvases; never clip, truncate, or place content beneath
   the footer.
5. Prove generalization with ten non-Amazon archetypes, all existing tests, 3200 px
   portfolio/general renders, side-by-side real-image checks when local source
   images exist, code review, security checks, and clean merge/push verification.
